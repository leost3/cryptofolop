import styled from 'styled-components'

export const Card = styled.div.attrs({
  className:
    'flex flex-col items-center justify-center space-y-10 mb-8 rounded-xl p-4 flex-1 justify-between md:flex-row '
} as { className: string })``
